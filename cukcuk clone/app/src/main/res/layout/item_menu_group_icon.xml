<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="64dp"
    android:paddingStart="8dp"
    android:paddingEnd="8dp">

    <ImageView
        android:id="@+id/ivSelectedGroup"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_margin="@dimen/dp_8"
        android:background="@drawable/circle_fill_blue"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription" />

    <ImageView
        android:id="@+id/ivUnselectedGroup"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/circle"
        app:layout_constraintBottom_toBottomOf="@id/ivSelectedGroup"
        app:layout_constraintEnd_toEndOf="@id/ivSelectedGroup"
        app:layout_constraintStart_toStartOf="@id/ivSelectedGroup"
        app:layout_constraintTop_toTopOf="@id/ivSelectedGroup"
        tools:ignore="ContentDescription" />

    <ImageView
        android:id="@+id/ivGroupIcon"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_margin="@dimen/dp_4"
        app:layout_constraintBottom_toBottomOf="@id/ivSelectedGroup"
        app:layout_constraintEnd_toEndOf="@id/ivSelectedGroup"
        app:layout_constraintStart_toStartOf="@id/ivSelectedGroup"
        app:layout_constraintTop_toTopOf="@id/ivSelectedGroup"
        tools:ignore="ContentDescription"
        tools:src="@drawable/ic_kem" />
</androidx.constraintlayout.widget.ConstraintLayout>
